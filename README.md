# Metabase with SQlite

## What's this

Docker compose stack of Metabase + a SQLite database in a folder that gets shared to the container

## How to run

- just "docker compose up" on the main folder

## Requirements

Docker